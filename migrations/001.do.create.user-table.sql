CREATE TABLE character_creater_users (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_name TEXT NOT NULL,
  password TEXT NOT NULL,
  deleted BOOLEAN NOT NULL,
  email TEXT NOT NULL
);